{{template "layout.tmpl" .}}

<div class="container mt-4">
  <h1 class="mb-3">Товары</h1>

  <a href="/admin/ingredients/new" class="btn btn-primary mb-3">+ Новый товар</a>

  <table class="table table-striped table-bordered align-middle">
    <thead class="table-light">
      <tr>
        <th>Название</th>
        <th>Отдел</th>
        <th style="width: 120px;"></th>
      </tr>
    </thead>
    <tbody>
      {{range .Items}}
      <tr>
        <td>{{.Name}}</td>
        <td>{{.Department}}</td>
        <td>
          <a href="/admin/ingredients/edit?id={{.ID}}" class="btn btn-sm btn-outline-secondary">Править</a>
        </td>
      </tr>
      {{else}}
      <tr>
        <td colspan="3" class="text-center text-muted">Нет товаров</td>
      </tr>
      {{end}}
    </tbody>
  </table>
</div>
